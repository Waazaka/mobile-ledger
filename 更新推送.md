# 更新与推送

此目录是独立 Git 仓库，已关联公开 GitHub 项目。后续开发后在 PowerShell 执行：

```powershell
cd 'D:\dhs\创业贷款\手机账本'
git status
git add README.md server.mjs test.mjs .gitignore .env.example
git commit -m "描述本次修改"
git push
```

新增/修改普通项目文件时检查 `git status` 后一并 `git add <文件>`。不要提交 `data/`、真实 `.env`、API Key、账单图片或账本数据。若数据库格式有变更，提交前先做本机数据备份。只有当前仓库提交会被推送；父目录的规划资料不会上传。
